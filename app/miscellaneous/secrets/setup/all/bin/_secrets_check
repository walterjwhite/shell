#!/bin/sh

[ -z "$1" ] && exit_with_error "\$1 is empty"

secret_value=$(secrets -out=stdout get $1 8>/dev/null) || {
  exit_with_error "unable to retrieve secret $1"
}

[ "${#secret_value}" -eq 0 ] && exit_with_error "$1 is empty"
