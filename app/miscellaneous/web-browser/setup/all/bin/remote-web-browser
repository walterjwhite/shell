#!/bin/sh

lib ./arguments.sh
lib git/data.app.sh
lib ./history.sh

cfg git

web_browser_remote_debug=$(awk -v min=5 -v max=65535 'BEGIN{srand(); print int(min+rand()*(max-min+1))}')

_CONSOLE_CONTEXT_ID=$(_random)

_data_app_init

_browser_configure
web_browser_launch
