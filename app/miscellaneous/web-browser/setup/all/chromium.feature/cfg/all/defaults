

case $APP_PLATFORM_PLATFORM in
Linux | FreeBSD)
  _CONFIGURATION_DIRECTORY=$HOME/.config/chromium
  ;;
*)
  exit_with_error "unsupported platform: $APP_PLATFORM_PLATFORM"
  ;;
esac

_CONFIGURATION_DIRECTORY=$HOME/.config/chromium
