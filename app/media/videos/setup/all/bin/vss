#!/bin/sh

lib ./videos.sh

cfg git

sudo_ensure_not_root

_data_app_init

find watched -type f -exec head -1 {} \; | cut -f3 -d'|' |
  $GNU_GREP -P '[\d]{2}\:[\d]{2}\:[\d]{2}' |
  awk -F':' '{sum += $1*3600 + $2*60 + $3;} END{print sum/3600}'
