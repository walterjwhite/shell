#!/bin/sh

_ROOT=/media/raw
cd $_ROOT

_INDEX=0

cd .git/annex/objects

while read _ANNEX_LINE; do
	if [ -z "$_ANNEX_LINE" ]; then
		continue
	fi

	_DISC_PATH=$_ROOT/discs/$_INDEX
	mkdir -p $_DISC_PATH

	mv $_ANNEX_LINE $_DISC_PATH

	_INDEX=$((_INDEX + 1))
done </media/raw-backup/index
