from PIL import Image
lib sys

if len(sys.argv) != 2:
	print('ERROR: input file is required')
	sys.exit(1)

fi = sys.argv[1]
fo = fi[:-3] + 'jpg'

if fi == fo:
	print('ERROR:' + fi + '->' + fo)
	sys.exit(2)

im = Image.open(fi)
im.convert('RGB').save(fo, 'JPEG')
