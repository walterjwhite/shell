#!/bin/sh

lib time.sh

case $_SUB_PLATFORM in
    Ubuntu)
        _SUDO_REQUIRED=1 _timeout $_CONF_INSTALL_STEP_TIMEOUT "snap install gitkraken" snap install gitkraken
        ;;
    *)
        _WARN "Unsupported platform $_PLATFORM"
        ;;
esac
