#!/bin/sh

lib ./add.sh
lib git/data.app.sh

cfg git

_git_init

if [ "$#" -lt "2" ]; then
	_ERROR "Reference key and reference required (file, url, text, gist, git project)" 1
fi

_KEY=$1
shift

for _ARG in "$@"; do
	_add "$_ARG"
done
