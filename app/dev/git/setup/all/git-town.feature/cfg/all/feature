if command -v Xwayland >/dev/null 2>&1; then
  :
elif command -v Xorg >/dev/null 2>&1; then
  :
else
  log_warn "disabling git-town as no graphical environment was detected"
  feature_git_town_disabled=1
fi
