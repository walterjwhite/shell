#!/bin/sh

lib net/network.sh

log_info "_dhcpcd-callback: $interface | $reason | $ifcarrier | $ifup"

_online && exit_with_success "connected to the Internet, no captive portal detected"

captive-portal-auto-accept
_online
