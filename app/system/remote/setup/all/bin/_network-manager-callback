#!/bin/sh

readonly REQUIRED_ARGUMENTS="_DEVICE:Device|_EVENT:Event"

lib net/network.sh

case "$_EVENT" in
up | dhcp4-change)
  _online || {
    captive-portal-auto-accept
  }
  ;;
*)
  log_warn "ignoring: $_DEVICE -> $_EVENT"
  ;;
esac
