_has_zfs() {
	which zfs >/dev/null 2>&1 || return 1
	
	return 0
}

_has_zfs

: ${feature_zfs_disabled:=$?}

: ${conf_system_maintenance_zfs_backup_path:=/var/tmp/backups}
: ${conf_system_maintenance_zfs_user:=zap}
