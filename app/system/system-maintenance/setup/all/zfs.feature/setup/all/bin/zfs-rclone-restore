#!/bin/sh

lib feature:zfs.rclone.sh
lib fs/zfs.sh

zfs_rclone_restore() {
  log_warn "overwriting $_ZFS_VOLUME"
  rclone cat $_rclone_target:$_ZFS_VOLUME | zfs receive -F $_ZFS_VOLUME
}

_zfs_rclone restore
