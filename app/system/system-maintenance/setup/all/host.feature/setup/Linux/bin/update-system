#!/bin/sh

lib alert.sh
lib install
lib io/write.sh
lib system-alert.sh

package_update

app-update

root_zfs_volume=$(zfs get -H name / | awk {'print$1'} | cut -f1 -d/ | head -1)

zap snap 30d ${root_zfs_volume}
